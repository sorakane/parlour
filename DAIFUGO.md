# 日本の大富豪 — ローカルルール拡張

`braedonsaunders/parlour` の MIT ライセンスと既存ゲームを維持した fork です。
元の President は変更せず、`packages/game-daifugo` を追加しています。

- 大富豪の設定: `/daifugo/`
- CPU 対戦: `/daifugo/table/`
- 友人用の部屋: `/daifugo/create/`
- ゲーム一覧: `/games/`

## 実装済み

4〜8人、同じ数字の組、0〜2枚のジョーカー、パス、場流し、順位、次ゲームのカード交換、CPU 3段階に対応します。
次のルールを設定画面から変更できます。

| 設定         | 動作                                                                    |
| ------------ | ----------------------------------------------------------------------- |
| 革命         | 有効/無効、発動枚数4〜6枚                                               |
| 8切り        | 実際の8を含む組で場を流す                                               |
| 11バック     | 実際のJを含む組ごとに反転、場流しで解除                                 |
| スート縛り   | 同じスートの組が2回続くと成立                                           |
| スペ3返し    | 単体ジョーカーに♠3を返して場を流す                                      |
| 5スキップ    | 実際の5の枚数ぶん次の応答者を飛ばす                                     |
| パス         | 場流しまで復帰不可、または他人が出した後に復帰                          |
| 交換         | 有効/無効、大貧民の献上枚数1〜3枚。貧民は1枚少ない                      |
| 最初の親     | ランダム、または♦3を持つ人（♦3を含めて出す）                            |
| 次ゲームの親 | 前回最下位、前回1位、ランダム                                           |
| 手番順       | 固定、毎ゲームランダム、前回順位順                                      |
| マッチ終了   | 順位ポイントの目標5〜40                                                 |
| 階段         | 同スートの連番、最小3〜5枚。2、途中のジョーカー、階段の重なりを個別設定 |
| 階段革命     | 通常革命とは別にON/OFF、発動枚数4〜13枚                                 |
| 階段内の効果 | 8切り・11バック・5スキップ・7渡し・10捨てをまとめてON/OFF               |
| 激縛り       | 同じスートと連続する数字で成立。以後も次の数字が必要                    |
| 7渡し        | 実際の7の枚数ぶん選択し、手番順で次の未終了者へ渡す                     |
| 10捨て       | 実際の10の枚数ぶん、手札から選んで捨てる                                |
| 都落ち       | 前の大富豪以外が最初に正常に上がると、前の大富豪が脱落                  |
| 禁止上がり   | ジョーカー、8切り、最強札、スペ3返し、効果による0枚を個別設定           |

ジョーカーは通常の組の数字を補います。ジョーカーだけの組は常に強く、同じ枚数のジョーカー組は上書きできません。
ジョーカーによる数字代用では8切り・11バック・5スキップ・7渡し・10捨ては発動しません。
革命と11バックは排他的論理和で強弱を決め、両方有効なら通常順です。
同ランクの組にジョーカーを含む場合は縛りが新規成立せず、成立済みの縛りでは不足スートを補えます。階段のジョーカーは実札と同じスートです。

### 今回採用したローカルルールの扱い

- 新しい「ローカル卓」プリセットで主要な追加ルールをまとめて有効にできます。従来の「定番」や保存済みルールは追加機能OFFで互換性を保ちます。
- 階段は同じ枚数・種類で返し、最小ランクで比較します。2はAの次に限り、A・2・3にはつながりません。ジョーカーは途中の欠番だけ補い、端の数字指定には未対応です。
- 激縛りは通常のスート縛りとは別にON/OFFできます。逆転中は数字の進む向きも逆です。階段の重なり禁止時は、隣接する重ならない階段で続けます。
- 同時発動は7渡し→10捨て→上がり判定→8切り/次の手番です。渡し先は終了者を飛ばし、パス中の人には渡せます。手札が足りなければ残り全部、なくなれば残りの選択を省略します。選んで渡す/捨てる札の追加効果は発動しません。
- 禁止札でもプレイ可能にして、最後に禁止札しかない場合の進行停止を防ぎます。反則者は脱落し、先に脱落した人ほど下位です。都落ちも同じ順位処理です。反則上がりでは都落ちを起こしません。
- 最強札の禁止は出す直前の強さで判定します（通常2、逆転中3）。7渡し/10捨ての選択で手札が0枚になる場合は、出す札の数字とは別の設定です。
- 効果の選択中も、必要枚数・渡し先・残り効果をイベント再生対象の状態に保持します。再接続・ホスト移行後に同じ選択から続行できます。

手番順の変更はプレイヤーIDを維持し、手札や接続中の席を入れ替えません。現在の順序は対局画面に表示します。
名前付きルールは最大20件、同じブラウザのローカルストレージに保存できます。アカウントやDBは追加していません。
大富豪固有の説明と設定は日本語です。共通メニューには既存の英語等の表示が残っています。

## 調査と設計

- `packages/engine`: 決定的な seed・イベントログ・state hash、合法手、playerView、演出イベントを共用。
- `packages/game-president`: 配札、順位、カード交換、ターン型ゲームの実装を参考・再利用。元パッケージは無変更。
- `packages/game-daifugo/src/combinations.ts`: 同ランク・ジョーカー・強弱・縛りの判定を分離。エンジンと画面が同じ関数を使う。
- `config.ts`: 既存 configSchema を使い、プリセット、入力補正、部屋に渡す設定を統一。
- `apps/web/src/lib/rooms`: ゲームID、人数、ルート、ランタイムを登録。
- `apps/web/src/lib/games/tablePacks/daifugo.tsx`: CPUと友人対戦を既存の共通テーブルに接続。
- `apps/web/src/app/_multiplayer/roomSession.ts`: 部屋作成、WebRTC、再参加、ホスト移行、再戦を共用。
- `apps/web`: Next.js の static export。`vercel.json` はリポジトリルートの既存設定を使用。

通常ルールの追加は、config → state（持続する効果のみ）→ 組の判定/効果 → CPU → 説明/テストの順に行います。
`StandingSet.kind` で同ランクの組と階段を区別します。効果判定は `effects.ts` に分離し、画面・CPU・エンジンで共用します。

## オンライン対戦の境界

Vercelは静的ファイルを配信し、対局状態は既存P2P通信とイベントログで共有します。
シグナリングには既存の外部Nostrリレー、接続にはWebRTCを利用します。外部リレーや端末のネットワーク条件にも依存するため、どの回線でも接続できる保証はありません。

大富豪は既存の **open room** を利用します。通常UIでは他人の手札を表示しませんが、暗号学的に隠すVeilモードは未対応で、参加者の端末には再生可能な状態が渡ります。友人同士を想定し、賞金付き等の不正対策用途には適しません。
他の全ゲームのVeil設定は維持しています。Veil非対応パッケージで既定値だけがVeilになる不整合を修正し、明示的にopenへ解決します。

再参加には同じブラウザのプロフィールと元の部屋コード/URLを使います。全員がブラウザを閉じた後の復旧は保証しません。

## 検証

今回の確認（2026-09-06）: 全パッケージテストでゲーム/エンジン1,453件、Web1,822件、対戦シナリオ10件が成功。その後、激縛りのみ有効時の回帰テストを1件加え、大富豪51件が成功しています（ゲーム/エンジン計1,454件）。低頻度の長時間シミュレーション等の既存skipは維持。lint、型チェックと静的本番ビルドも成功しました。

- 追加ルールの組み合わせ、4〜8人の全ルールCPU完走、カード総数保存、イベント再生とhash一致を検証。
- 7渡し・10捨ての必要枚数の選択、確認ボタン、画面から有効な手の送信、保存済みルールの互換性を検証。
- 通常手・7渡し選択中・10捨て選択中の3ケースで、ゲストの新しいキーでの再参加→ホスト離脱→選択の続行→全員の状態とログ一致を確認。
- 再参加後の操作IDが以前の操作と衝突し、無視される共通セッションの不具合を修正。操作IDにセッションごとのUUIDを使用します。RTCモックも切断したチャンネルが送受信を続けないよう、実際の切断に合わせました。
- 390px幅で新プリセットのCPU対戦、CPUの階段・階段革命・10捨ての進行と、人が7渡しのカードを選んで渡す操作を確認。大富豪専用の場札表示を中央へ寄せ、長い階段も画面内へ収めました。

以下は初期実装時の検証記録です。

途中の実行では音楽テスト4件と既存Wildの暗号化ホスト離脱テスト1件が一時的に失敗しました。音楽は個別再実行と全Web再実行で成功。Wildは変更前コミットで個別/シナリオ全件に成功し、変更後も対戦シナリオ全件の再実行で成功しました。原因が確定した修正としては扱わず、間欠的な検証上の課題として記録します。大富豪の移行テストは全参加者のホスト合意を待つよう修正しました。

- 元のコードで依存導入と全パッケージビルドに成功し、変更前のサイトをVercelへ配信。
- 大富豪のユニットテスト: 特殊ルールの組合せ、交換、無効手、カード保存、4〜8人のCPU完走、イベント再生後のhash一致。
- 部屋の統合テスト: 4人で18手同期 → ゲストの新セッションが同じ席へ再参加 → 重複席なし → ホスト離脱・移行 → 続行 → 全員の盤面とログ一致。
- 統合テストはシグナリング/RTCのモックを使用。実回線をまたぐ複数端末、iPhone Safariのバックグラウンド復帰は別途実機確認が必要。
- 実ブラウザ: 名前付きルールの保存・リロード、CPU対戦、同ランク2枚出し、390px幅の対局画面を確認。実リレー経由の部屋作成、空席へのCPU追加、ルーム内の11バック・縛りも確認。
- ベースラインの `roomSession.test.ts` にあった4席の暗号化配札テストの5秒制限超過に対し、その1件のみ30秒へ延長。検証内容は維持。

再現コマンド（Node.jsとpnpm 10.30.3）:

```sh
pnpm install --frozen-lockfile
pnpm -r test
pnpm lint
pnpm -r build
pnpm --filter @parlour/web serve:export
```

この開発環境ではTurbopackのCSS処理用ポートに実行制限があるため、最終配信物は `pnpm --filter @parlour/web exec next build --webpack` と `pnpm --filter @parlour/web postbuild` で生成しました。全ゲームパッケージの型ビルドは成功しています。

VercelのGit連携では、このforkのリポジトリルートをImportします。Build Commandは `pnpm --filter @parlour/web... build`、Output Directoryは `apps/web/out` です。

## 次段階

Phase1に続き、主要ローカルルール（Phase2）と、その相互作用・都落ち・禁止上がりなどPhase3の一部を実装しました。全フェーズ完了ではありません。以下は未実装・未検証です。

1. 階段の端に置くジョーカーの数字指定、さらに異なる地域独自の縛りや効果の派生。
2. 単体ジョーカーの強さを変更する設定、ホスト操作による任意の席順シャッフル。
3. ロビーの席の物理的な並べ替え、ルール共有/インポート。
4. 実回線・複数端末でリロード、一時オフライン、ホスト離脱、再戦を検証。
5. 必要に応じた日本語の共通UI、Veil対応の別途設計・監査。
